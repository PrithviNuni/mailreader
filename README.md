This project has been created for reading mails from mail server

Build commands

mvn clean mvn install

java -jar target/MailReader-0.0.1-SNAPSHOT.jar

Load following url in a browser

http://:8090/copyMail
